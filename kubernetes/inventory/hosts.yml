all:
  children:
    k8s_cluster:
      children:
        control_plane:
          hosts:
            homelab1:
              ansible_host: 192.168.1.21
        workers:
          hosts:
            homelab2:
              ansible_host: 192.168.1.22
            homelab3:
              ansible_host: 192.168.1.23
  vars:
    ansible_user: ansible
    ansible_python_interpreter: /usr/bin/python3
    # SSH and sudo passwords can be provided at runtime:
    #   ansible-playbook site.yml --ask-pass --ask-become-pass
    # Or use SSH keys and passwordless sudo for automated runs
